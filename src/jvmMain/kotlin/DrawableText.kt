class DrawableText {
    // TODO: something with its own textmeasurer, and a draw method that uses it
    // Instances of this class should be remembered rather than the instances remembering anything themselves (they're not composables after all)
}